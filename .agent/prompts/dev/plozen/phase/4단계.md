# 4단계: 성능 및 SEO 최적화

## 🎯 단계 목표

웹사이트의 성능을 극대화하고 검색 엔진 최적화를 통해 가시성을 높입니다. Core Web Vitals 점수 90점 이상을 목표로 합니다.

## 📅 예상 기간: 2-3일

## 🎯 주요 과업

### 1. 동적 메타데이터 구현

**목표:** 각 페이지에 최적화된 SEO 메타데이터 동적 생성

#### 구현 항목:

- [ ] **동적 타이틀:** 페이지 내용 기반 타이틀 생성
- [ ] **메타 설명:** 검색 결과 최적화된 설명
- [ ] **OG 태그:** 소셜 미디어 공유 최적화
- [ ] **구조화 데이터:** JSON-LD 형식 마크업
- [ ] **사이트맵:** 동적 XML 사이트맵 생성
- [ ] **로봇.txt:** 검색 엔진 크롤링 가이드

#### 메타데이터 생성:

```typescript
// lib/metadata.ts
interface PageMetadata {
  title: string;
  description: string;
  keywords?: string[];
  ogImage?: string;
  canonical?: string;
  noindex?: boolean;
}

export const generateMetadata = (page: PageMetadata): Metadata => {
  return {
    title: `${page.title} | Plozen - 웹 에이전시`,
    description: page.description,
    keywords: page.keywords?.join(', '),
    openGraph: {
      title: page.title,
      description: page.description,
      type: 'website',
      url: page.canonical,
      images: page.ogImage ? [{ url: page.ogImage }] : [],
      siteName: 'Plozen',
    },
    twitter: {
      card: 'summary_large_image',
      title: page.title,
      description: page.description,
      images: page.ogImage ? [page.ogImage] : [],
    },
    canonical: page.canonical,
    robots: {
      index: !page.noindex,
      follow: !page.noindex,
    },
  };
};

// 페이지별 메타데이터
export const pageMetadata: Record<string, Omit<PageMetadata, 'canonical'>> = {
  home: {
    title: '프로페셔널 웹 개발 | Plozen',
    description:
      '최신 기술로 완성도 높은 웹사이트를 제작합니다. 기업 홈페이지, 랜딩페이지, 웹 애플리케이션 제작 전문.',
    keywords: ['웹개발', '웹에이전시', '홈페이지제작', 'Next.js', 'React'],
  },
  services: {
    title: '서비스 소개 | Plozen',
    description:
      '웹사이트 제작, 웹 애플리케이션 개발, UI/UX 디자인, 유지보수까지 완벽한 솔루션을 제공합니다.',
    keywords: ['웹사이트제작', '앱개발', 'UIUX디자인', '유지보수'],
  },
  // ... 다른 페이지들
};
```

#### 구조화 데이터:

```typescript
// lib/structuredData.ts
export const generateOrganizationData = () => ({
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'Plozen',
  url: 'https://plozen.com',
  logo: 'https://plozen.com/logo.png',
  description: '프로페셔널 웹 개발 에이전시',
  contactPoint: {
    '@type': 'ContactPoint',
    telephone: '+82-10-1234-5678',
    contactType: 'customer service',
  },
  sameAs: ['https://github.com/plozen', 'https://linkedin.com/company/plozen'],
});

export const generateServiceData = (service: Service) => ({
  '@context': 'https://schema.org',
  '@type': 'Service',
  name: service.name,
  description: service.description,
  provider: {
    '@type': 'Organization',
    name: 'Plozen',
  },
  offers: {
    '@type': 'Offer',
    price: service.price,
    priceCurrency: 'KRW',
  },
});
```

### 2. 이미지 최적화 및 Core Web Vitals

**목표:** Lighthouse 점수 90점 이상 달성

#### 구현 항목:

- [ ] **Next.js Image:** 모든 이미지 최적화
- [ ] **이미지 포맷:** WebP, AVIF 지원
- [ ] **레이지 로딩:** 이미지 지연 로딩
- [ ] **CLS 최적화:** 레이아웃 시프트 방지
- [ ] **LCP 최적화:** 가장 큰 콘텐츠 페인트 개선
- [ ] **FID 최적화:** 첫 입력 지연 개선

#### 이미지 최적화:

```typescript
// components/OptimizedImage.tsx
interface OptimizedImageProps {
  src: string;
  alt: string;
  width?: number;
  height?: number;
  priority?: boolean;
  className?: string;
  sizes?: string;
}

export const OptimizedImage: FC<OptimizedImageProps> = ({
  src,
  alt,
  width,
  height,
  priority = false,
  className,
  sizes = '(max-width: 768px) 100vw, 50vw'
}) => {
  return (
    <div className={cn('relative overflow-hidden', className)}>
      <Image
        src={src}
        alt={alt}
        width={width}
        height={height}
        priority={priority}
        sizes={sizes}
        className="object-cover transition-transform duration-300 hover:scale-105"
        placeholder="blur"
        blurDataURL="data:image/jpeg;base64,"
      />
    </div>
  );
};
```

#### 성능 모니터링:

```typescript
// lib/performance.ts
export const reportWebVitals = (onPerfEntry?: (metric: any) => void) => {
  if (onPerfEntry && onPerfEntry instanceof Function) {
    import('web-vitals').then(({ getCLS, getFID, getFCP, getLCP, getTTFB }) => {
      getCLS(onPerfEntry);
      getFID(onPerfEntry);
      getFCP(onPerfEntry);
      getLCP(onPerfEntry);
      getTTFB(onPerfEntry);
    });
  }
};

// 성능 메트릭 수집
export const trackPerformance = (metric: any) => {
  // Google Analytics에 전송
  if (window.gtag) {
    window.gtag('event', metric.name, {
      event_category: 'Web Vitals',
      value: Math.round(metric.name === 'CLS' ? metric.value * 1000 : metric.value),
      event_label: metric.id,
      non_interaction: true,
    });
  }

  // 콘솔에 기록 (개발 환경)
  if (process.env.NODE_ENV === 'development') {
    console.log(`[Web Vitals] ${metric.name}:`, metric.value, metric);
  }
};
```

### 3. 코드 분할 및 번들 최적화

**목표:** 초기 로딩 시간 최소화 및 번들 사이즈 최적화

#### 구현 항목:

- [ ] **동적 임포트:** 라우트별 코드 분할
- [ ] **컴포넌트 분할:** 무거운 컴포넌트 지연 로딩
- [ ] **번들 분석:** Webpack Bundle Analyzer
- [ ] **트리 쉐이킹:** 사용하지 않는 코드 제거
- [ ] **외부 라이브러리:** CDN 활용
- [ ] **폰트 최적화:** 폰트 서브셋 및 프리로드

#### 동적 임포트:

```typescript
// 동적 라우트 로딩
const AdminDashboard = dynamic(() => import('@/app/(admin)/admin/page'), {
  loading: () => <DashboardSkeleton />,
  ssr: false // 클라이언트 사이드에서만 로드
});

const QuoteForm = dynamic(() => import('@/components/templates/Quote/QuoteForm'), {
  loading: () => <FormSkeleton />,
  ssr: true // SEO를 위해 SSR
});

// 컴포넌트 지연 로딩
const HeavyChart = lazy(() => import('@/components/charts/HeavyChart'));

// 조건부 로딩
const AdminPanel = ({ isAdmin }: { isAdmin: boolean }) => {
  if (!isAdmin) return null;

  return (
    <Suspense fallback={<div>Loading admin panel...</div>}>
      <HeavyChart />
    </Suspense>
  );
};
```

#### 번들 최적화 설정:

```typescript
// next.config.ts
const nextConfig = {
  experimental: {
    optimizeCss: true,
    optimizePackageImports: ['lucide-react', 'framer-motion'],
  },
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production',
  },
  images: {
    formats: ['image/webp', 'image/avif'],
    deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048, 3840],
    imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  },
  webpack: (config, { isServer }) => {
    // 클라이언트 사이드에서만 사용되는 패키지 분리
    if (!isServer) {
      config.resolve.fallback = {
        ...config.resolve.fallback,
        fs: false,
        net: false,
        tls: false,
      };
    }

    // 번들 분석
    if (process.env.ANALYZE) {
      const { BundleAnalyzerPlugin } = require('webpack-bundle-analyzer');
      config.plugins.push(new BundleAnalyzerPlugin());
    }

    return config;
  },
};
```

### 4. 검색 엔진 최적화

**목표:** 구글 검색 상위 노출 및 가시성 확보

#### 구현 항목:

- [ ] **키워드 최적화:** 타겟 키워드 분석 및 적용
- [ ] **내부 링크:** 페이지 간 연결 강화
- [ ] **외부 링크:** 신뢰도 높은 사이트 연결
- [ ] **속도 최적화:** 페이지 속도 개선
- [ ] **모바일 최적화:** 반응형 디자인 완성
- [ ] **로컬 SEO:** 지역 기반 검색 최적화

#### SEO 최적화 컴포넌트:

```typescript
// components/SEOHead.tsx
interface SEOHeadProps {
  title: string;
  description: string;
  keywords?: string[];
  ogImage?: string;
  canonical?: string;
  structuredData?: Record<string, any>;
}

export const SEOHead: FC<SEOHeadProps> = ({
  title,
  description,
  keywords,
  ogImage,
  canonical,
  structuredData
}) => {
  const metadata = generateMetadata({
    title,
    description,
    keywords,
    ogImage,
    canonical
  });

  return (
    <>
      <Head>
        {Object.entries(metadata).map(([key, value]) => (
          <meta key={key} name={key} content={value as string} />
        ))}

        {structuredData && (
          <script
            type="application/ld+json"
            dangerouslySetInnerHTML={{
              __html: JSON.stringify(structuredData)
            }}
          />
        )}
      </Head>
    </>
  );
};
```

## 🛠️ 성능 측정 도구

### 1. Lighthouse 자동화

```typescript
// scripts/lighthouse.js
const lighthouse = require('lighthouse');
const chromeLauncher = require('chrome-launcher');

const runLighthouse = async (url) => {
  const chrome = await chromeLauncher.launch({ chromeFlags: ['--headless'] });
  const options = {
    logLevel: 'info',
    output: 'json',
    onlyCategories: ['performance', 'accessibility', 'best-practices', 'seo'],
    port: chrome.port,
  };

  const runnerResult = await lighthouse(url, options);
  await chrome.kill();

  return runnerResult.lhr;
};

// CI/CD 파이프라인에서 실행
if (process.env.CI) {
  runLighthouse('https://plozen.com').then((results) => {
    const score = results.categories.performance.score * 100;
    if (score < 90) {
      console.error(`Performance score too low: ${score}`);
      process.exit(1);
    }
  });
}
```

### 2. 실시간 성능 모니터링

```typescript
// hooks/usePerformanceMonitoring.ts
export const usePerformanceMonitoring = () => {
  useEffect(() => {
    // 페이지 로드 시간 측정
    const navigation = performance.getEntriesByType('navigation')[0] as PerformanceNavigationTiming;
    const loadTime = navigation.loadEventEnd - navigation.loadEventStart;

    // API 응답 시간 모니터링
    const originalFetch = window.fetch;
    window.fetch = async (...args) => {
      const start = performance.now();
      const response = await originalFetch(...args);
      const duration = performance.now() - start;

      // 느린 API 요청 로깅
      if (duration > 1000) {
        console.warn(`Slow API request: ${args[0]} took ${duration}ms`);
      }

      return response;
    };

    return () => {
      window.fetch = originalFetch;
    };
  }, []);
};
```

## ✅ 완료 기준

### 성능 기준:

- [ ] Lighthouse 점수 90점 이상 (모든 카테고리)
- [ ] First Contentful Paint < 1.5초
- [ ] Largest Contentful Paint < 2.5초
- [ ] Cumulative Layout Shift < 0.1
- [ ] First Input Delay < 100ms

### SEO 기준:

- [ ] 모든 페이지 메타데이터 완성
- [ ] 구조화 데이터 100% 적용
- [ ] 사이트맵 자동 생성 및 제출
- [ ] 검색 엔진 색인 100% 허용
- [ ] Core Web Vitals 좋음 등급

### 기술적 기준:

- [ ] 번들 사이즈 1MB 미만 (gzip 압축)
- [ ] 이미지 WebP/AVIF 100% 적용
- [ ] 코드 분할 모든 라우트 적용
- [ ] 폰트 로딩 100ms 이내

## 🚀 다음 단계 준비

- 5단계 고급 기능을 위한 성능 기반 확보
- 실시간 기능을 위한 WebSocket 아키텍처 준비
- 파일 업로드를 위한 스토리지 최적화 기반 구축

---

_본 단계가 완료되면 검색 엔진 최적화와 성능이 극대화되어 사용자 경험이 크게 향상됩니다_
