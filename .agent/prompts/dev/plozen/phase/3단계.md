# 3단계: UX/UI 개선

## 🎯 단계 목표

사용자 경험을 극대화하고, 로딩 상태, 에러 처리, 알림 시스템 등 완성도 높은 UI/UX를 구현합니다.

## 📅 예상 기간: 2-3일

## 🎯 주요 과업

### 1. 로딩 상태 및 스켈레톤 UI

**목표:** 사용자가 기다리는 동안의 경험 개선

#### 구현 항목:

- [ ] **글로벌 로딩:** 페이지 전환 로딩 바
- [ ] **스켈레톤 UI:** 리스트, 카드, 폼 로딩 상태
- [ ] **버튼 로딩:** 클릭 후 로딩 스피너
- [ ] **이미지 로딩:** 블러 효과와 프로그레스
- [ ] **테이블 로딩:** 행 단위 스켈레톤
- [ ] **폼 로딩:** 입력 필드 로딩 상태

#### 스켈레톤 컴포넌트:

```typescript
// components/ui/Skeleton.tsx
interface SkeletonProps {
  className?: string;
  variant?: 'text' | 'circular' | 'rectangular';
  width?: string | number;
  height?: string | number;
  lines?: number; // text variant용
}

export const Skeleton: React.FC<SkeletonProps> = ({
  className,
  variant = 'text',
  width,
  height,
  lines = 1
}) => {
  return (
    <div className={cn('animate-pulse', className)}>
      {variant === 'text' && (
        <div className="space-y-2">
          {Array.from({ length: lines }).map((_, i) => (
            <div
              key={i}
              className="h-4 bg-gray-200 rounded"
              style={{
                width: i === lines - 1 ? '70%' : '100%',
                height: height || '1rem'
              }}
            />
          ))}
        </div>
      )}
      {variant === 'circular' && (
        <div
          className="bg-gray-200 rounded-full"
          style={{ width, height }}
        />
      )}
      {variant === 'rectangular' && (
        <div
          className="bg-gray-200 rounded"
          style={{ width, height }}
        />
      )}
    </div>
  );
};
```

### 2. 에러 처리 및 예외 페이지

**목표:** 사용자 친화적인 에러 처리 및 복구 가이드

#### 구현 항목:

- [ ] **에러 바운더리:** React 에러 캐치 및 처리
- [ ] **404 페이지:** 존재하지 않는 페이지 안내
- [ ] **500 페이지:** 서버 에러 안내 및 복구
- [ ] **네트워크 에러:** 오프라인 상태 처리
- [ ] **API 에러:** 요청 실패 시 안내
- [ ] **폼 유효성:** 실시간 검증 및 에러 표시

#### 에러 바운더리:

```typescript
// components/ErrorBoundary.tsx
interface ErrorBoundaryState {
  hasError: boolean;
  error?: Error;
}

export class ErrorBoundary extends Component<
  PropsWithChildren<{}>,
  ErrorBoundaryState
> {
  constructor(props: PropsWithChildren<{}>) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error): ErrorBoundaryState {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: ErrorInfo) {
    console.error('Error caught by boundary:', error, errorInfo);
    // 에러 로깅 서비스에 전송
  }

  render() {
    if (this.state.hasError) {
      return <ErrorFallback error={this.state.error} />;
    }

    return this.props.children;
  }
}
```

### 3. 토스트/모달 알림 시스템

**목표:** 일관되고 사용하기 쉬운 알림 시스템

#### 구현 항목:

- [ ] **토스트 컴포넌트:** 성공/에러/경고/정보 알림
- [ ] **모달 컴포넌트:** 확인/경고/커스텀 모달
- [ ] **알림 관리자:** 전역 알림 상태 관리
- [ ] **알림 큐:** 중복 알림 처리
- [ ] **자동 닫기:** 시간 기반 알림 제거
- [ ] **위치 설정:** 화면 위치 선택 가능

#### 토스트 시스템:

```typescript
// hooks/useToast.ts
interface Toast {
  id: string;
  type: 'success' | 'error' | 'warning' | 'info';
  title: string;
  message?: string;
  duration?: number;
  action?: {
    label: string;
    onClick: () => void;
  };
}

const useToast = () => {
  const [toasts, setToasts] = useState<Toast[]>([]);

  const addToast = useCallback((toast: Omit<Toast, 'id'>) => {
    const id = generateId();
    const newToast = { ...toast, id };

    setToasts((prev) => [...prev, newToast]);

    // 자동 제거
    if (toast.duration !== 0) {
      setTimeout(() => {
        removeToast(id);
      }, toast.duration || 5000);
    }

    return id;
  }, []);

  const removeToast = useCallback((id: string) => {
    setToasts((prev) => prev.filter((toast) => toast.id !== id));
  }, []);

  return {
    toasts,
    addToast,
    removeToast,
    success: (title: string, message?: string) => addToast({ type: 'success', title, message }),
    error: (title: string, message?: string) =>
      addToast({ type: 'error', title, message, duration: 0 }),
    warning: (title: string, message?: string) => addToast({ type: 'warning', title, message }),
    info: (title: string, message?: string) => addToast({ type: 'info', title, message }),
  };
};
```

### 4. 다크모드 완성

**목표:** 완벽한 다크모드 지원 및 테마 전환

#### 구현 항목:

- [ ] **테마 프로바이더:** 전역 테마 상태 관리
- [ ] **CSS 변수:** 라이트/다크 모드 색상 정의
- [ ] **테마 토글:** 헤더 테마 스위치
- [ ] **시스템 테마:** OS 설정 자동 감지
- [ ] **테마 저장:** localStorage 영속성
- [ ] **전환 애니메이션:** 부드러운 테마 변경

#### 테마 시스템:

```typescript
// contexts/ThemeContext.tsx
type Theme = 'light' | 'dark' | 'system';

interface ThemeContextType {
  theme: Theme;
  resolvedTheme: 'light' | 'dark';
  setTheme: (theme: Theme) => void;
  toggleTheme: () => void;
}

export const ThemeProvider: FC<PropsWithChildren> = ({ children }) => {
  const [theme, setTheme] = useState<Theme>('system');
  const [resolvedTheme, setResolvedTheme] = useState<'light' | 'dark'>('light');

  useEffect(() => {
    const stored = localStorage.getItem('theme') as Theme;
    if (stored) setTheme(stored);
  }, []);

  useEffect(() => {
    const root = window.document.documentElement;

    if (theme === 'system') {
      const systemTheme = window.matchMedia('(prefers-color-scheme: dark)').matches
        ? 'dark'
        : 'light';
      setResolvedTheme(systemTheme);
      root.classList.remove('light', 'dark');
      root.classList.add(systemTheme);
    } else {
      setResolvedTheme(theme);
      root.classList.remove('light', 'dark');
      root.classList.add(theme);
    }
  }, [theme]);

  const toggleTheme = () => {
    setTheme(prev => prev === 'light' ? 'dark' : 'light');
  };

  return (
    <ThemeContext.Provider value={{ theme, resolvedTheme, setTheme, toggleTheme }}>
      {children}
    </ThemeContext.Provider>
  );
};
```

## 🎨 UI 컴포넌트 라이브러리 확장

### 1. 기본 컴포넌트

```typescript
// components/ui/index.ts
export { Button } from './Button';
export { Input } from './Input';
export { Modal } from './Modal';
export { Toast } from './Toast';
export { Skeleton } from './Skeleton';
export { LoadingSpinner } from './LoadingSpinner';
export { ErrorBoundary } from './ErrorBoundary';
```

### 2. 고급 컴포넌트

- [ ] **DataTable:** 정렬, 필터, 페이징
- [ ] **SearchBar:** 자동완성, 히스토리
- [ ] **FileUpload:** 드래그 앤 드롭, 프로그레스
- [ ] **DatePicker:** 달력, 범위 선택
- [ ] **RichTextEditor:** WYSIWYG 편집기

### 3. 애니메이션 라이브러리

```typescript
// animations/motion.ts
export const fadeIn = {
  initial: { opacity: 0 },
  animate: { opacity: 1 },
  exit: { opacity: 0 },
};

export const slideUp = {
  initial: { y: 20, opacity: 0 },
  animate: { y: 0, opacity: 1 },
  exit: { y: -20, opacity: 0 },
};

export const scaleIn = {
  initial: { scale: 0.9, opacity: 0 },
  animate: { scale: 1, opacity: 1 },
  exit: { scale: 0.9, opacity: 0 },
};
```

## 🛠️ 구현 방안

### 1. 상태 관리 통합

```typescript
// stores/uiStore.ts
interface UIState {
  isLoading: boolean;
  toasts: Toast[];
  modals: Modal[];
  theme: Theme;
}

export const useUIStore = create<UIState>((set, get) => ({
  isLoading: false,
  toasts: [],
  modals: [],
  theme: 'system',

  setLoading: (loading: boolean) => set({ isLoading: loading }),
  addToast: (toast: Toast) =>
    set((state) => ({
      toasts: [...state.toasts, toast],
    })),
  removeToast: (id: string) =>
    set((state) => ({
      toasts: state.toasts.filter((t) => t.id !== id),
    })),
  setTheme: (theme: Theme) => set({ theme }),
}));
```

### 2. 글로벌 스타일링

```scss
// styles/globals.scss
:root {
  // Light theme
  --bg-primary: #ffffff;
  --bg-secondary: #f8f9fa;
  --text-primary: #212529;
  --text-secondary: #6c757d;
  --border-color: #dee2e6;
  --accent: #007bff;
}

[data-theme='dark'] {
  // Dark theme
  --bg-primary: #1a1a1a;
  --bg-secondary: #2d2d2d;
  --text-primary: #ffffff;
  --text-secondary: #b3b3b3;
  --border-color: #404040;
  --accent: #4dabf7;
}

// Transitions
* {
  transition:
    background-color 0.3s ease,
    color 0.3s ease,
    border-color 0.3s ease;
}
```

### 3. 접근성 개선

```typescript
// hooks/useAccessibility.ts
export const useAccessibility = () => {
  const [reducedMotion, setReducedMotion] = useState(false);
  const [highContrast, setHighContrast] = useState(false);

  useEffect(() => {
    const motionQuery = window.matchMedia('(prefers-reduced-motion: reduce)');
    setReducedMotion(motionQuery.matches);

    const contrastQuery = window.matchMedia('(prefers-contrast: high)');
    setHighContrast(contrastQuery.matches);
  }, []);

  return { reducedMotion, highContrast };
};
```

## ✅ 완료 기준

### 사용성 기준:

- [ ] 로딩 상태가 모든 곳에서 표시됨
- [ ] 에러 발생 시 사용자 친화적 안내 제공
- [ ] 알림이 3초 내에 표시되고 상호작용 가능
- [ ] 다크모드 전환이 0.3초 내에 부드럽게 작동

### 접근성 기준:

- [ ] WCAG 2.1 AA 준수
- [ ] 키보드 네비게이션 모든 기능 지원
- [ ] 스크린 리더 호환
- [ ] 색상 대비율 4.5:1 이상

### 성능 기준:

- [ ] 로딩 애니메이션 60fps 유지
- [ ] 테마 전환 300ms 이내
- [ ] 알림 렌더링 100ms 이내
- [ ] 에러 페이지 로드 1초 이내

## 🚀 다음 단계 준비

- 4단계 성능 최적화를 위한 메트릭 수집 기반 구축
- SEO 최적화를 위한 메타데이터 동적 생성 준비
- 접근성 기준을 충족하는 HTML 구조 확립

---

_본 단계가 완료되면 사용자 경험이 크게 향상되고 프로페셔널한 완성도를 갖추게 됩니다_
