파트너님, **[Step 4]**는 사용자가 특정 회사를 클릭했을 때 그 회사에 투표한 팬들의 구성을 **다차원적으로 분석**하는 **'회사 상세 페이지(Company Detail)'**를 구현하는 단계입니다.

이 프롬프트는 AI(Plolux)에게 **"Next.js의 동적 라우팅을 활용하여, 회사별 투표 데이터를 아이돌 그룹/레이블/유저 차원으로 집계하고, 탭 UI와 댓글 기능으로 커뮤니티 상호작용을 만드는 상세 페이지를 구축하라"**고 지시합니다.

---

### **[Step 4: 회사 상세 페이지 및 다차원 투표 집계 분석]** 마스터 프롬프트

Markdown# [Step 4: 회사 상세 페이지 및 다차원 투표 집계 분석]

## 1. 목표

# [Step 4: 회사 상세 페이지 및 다차원 투표 집계 분석]

## 1. 목표

`/[locale]/ranking` 페이지의 회사 리스트에서 회사(예: 하이브)를 클릭하면 `/[locale]/company/[id]`로 이동하여, 그 회사에 투표한 팬들을 **3가지 관점(그룹/레이블/유저)으로 집계**하고 **탭 UI + 댓글 기능**으로 커뮤니티 상호작용을 제공합니다.

---

## 2. 구현 요구사항

### [Task 1] 투표 데이터 구조 정의

**파일**: `packages/kcl/src/lib/mock-data.ts`

Vote 인터페이스 추가:

```typescript
interface Vote {
  id: string;
  companyId: string; // 투표 대상 회사
  userId: string; // 투표한 유저
  groupId: string; // 유저가 팬인 아이돌 그룹
  labelId: string; // 그룹이 속한 레이블
  timestamp: number;
}

export const mockVotes: Vote[] = [
  // 최소 30개 이상의 투표 데이터
  // 특히 companyId: '1'(하이브)에 다양한 팬 분포
];
```

**데이터 설계**:

- 하이브(ID: '1')에 투표한 유저들: 뉴진스, 르세라핌, 벨플플 팬 섞임
- 각 그룹별 다양한 투표 분포 (예: 뉴진스 150표, 르세라핌 120표, 벨플플 80표)

---

### [Task 2] 동적 라우팅 페이지

**파일**: `packages/kcl/src/app/[locale]/company/[id]/page.tsx`

#### 1) Hero Header

- 회사명 + 순위 배지 (🏆 1위)
- 뒤로가기 버튼 (ChevronLeft 아이콘, `useRouter().back()` 또는 `router.push(\`/\${locale}/ranking\`)`)
- 총 투표 수 카운터 (숫자 애니메이션)
- Gradient 배경 (Violet → Cyan)
- Header 컴포넌트는 기존 `src/components/layout/Header` 재사용

#### 2) 탭 네비게이션 (3개 탭)

**탭 1: 그룹별 집계 (By Group)**

```
그룹명        투표 수   비율
뉴진스        150표    42.3%
르세라핌      120표    33.8%
벨플플        80표     22.6%
```

- 투표 수 내림차순 정렬
- 비율 계산: (그룹 투표 수 / 전체 투표 수) \* 100

**탭 2: 레이블별 집계 (By Label)**

```
레이블명       투표 수   비율
어도어         270표    76.1%
플레이리스트   50표     14.1%
소스뮤직       35표     9.9%
```

- 투표 수 내림차순 정렬

**탭 3: 유저별 집계 (By User)**

```
유저명         투표 수   선호 그룹
user123        5표      뉴진스
user456        3표      르세라핌
user789        2표      뉴진스
```

- 투표 수 내림차순 정렬 (다중 투표 유저 상단)

#### 3) 탭 UI 스펙

- 활성 탭: Primary Color 하단 보더 강조
- 탭 전환: `framer-motion` Fade-In 애니메이션
- 모바일: 카드 형식으로 변환
- Tabs 컴포넌트는 `src/components/common/Tabs/` 에서 재사용

---

### [Task 3] 댓글 기능

**파일**: `packages/kcl/src/components/features/comments/CommentSection/index.tsx`

#### 1) 댓글 입력 폼

- 유저명 입력 필드
- 댓글 내용 텍스트 영역 (최대 200자 제한)
- 등록 버튼 (Primary Button)
- 로컬 상태(`useState`)로 관리

#### 2) 댓글 리스트

- 최신 댓글 상단 표시
- 각 댓글: 유저명 | 시간 | 내용 | 삭제 버튼
- Mock 데이터 5~10개
- Glass Panel 스타일 + 좌측 보더 Primary Color

---

### [Task 4] 페이지 애니메이션

**구현 위치**: 페이지 최상단 `motion.div`

- **진입**: 오른쪽에서 왼쪽으로 슬라이드 (`x: '100%' → 0`)
- **퇴장**: 반대 방향 슬라이드

---

## 3. 디자인 스펙

| 항목        | 설정                                |
| ----------- | ----------------------------------- |
| Hero Header | Violet → Cyan 그래디언트            |
| Glass Panel | `rgba(10,10,10,0.8)` + `blur(20px)` |
| 테이블      | 호버 시 배경색 변화, 모바일은 카드  |
| 숫자 카운터 | 차오르는 애니메이션                 |

---

## 4. 중요 주의사항 ⚠️

### Locale 라우팅 유지

```typescript
// 뒤로가기 버튼 구현 시:
'use client';
import { useRouter, usePathname } from 'next/navigation';

const pathname = usePathname();
const locale = pathname.split('/')[1]; // 'en', 'ko' 등

// 뒤로가기 방법 1:
router.back();

// 뒤로가기 방법 2:
router.push(`/${locale}/ranking`);
```

### Header 컴포넌트 재사용

```typescript
import Header from '@/components/layout/Header';
// 페이지 상단에 포함
```

### Mock 데이터 일관성

- `mockCompanies`와 `mockVotes` 간 `companyId` 일치 필요
- `mockVotes`의 `userId`, `groupId`, `labelId`도 기존 mock 데이터와 일치

---

## 5. 파일 생성 체크리스트

- [ ] `packages/kcl/src/lib/mock-data.ts` - Vote 타입 & 데이터 추가
- [ ] `packages/kcl/src/app/[locale]/company/[id]/page.tsx` - 동적 라우팅 페이지
- [ ] `packages/kcl/src/app/[locale]/company/[id]/company-detail.module.scss`
- [ ] `packages/kcl/src/components/common/Tabs/index.tsx`
- [ ] `packages/kcl/src/components/common/Tabs/Tabs.module.scss`
- [ ] `packages/kcl/src/components/features/comments/CommentSection/index.tsx`
- [ ] `packages/kcl/src/components/features/comments/CommentSection/CommentSection.module.scss`

---

## 6. 데이터 흐름

```
1. 사용자가 /[locale]/ranking 페이지에서 회사 클릭
   ↓
2. /[locale]/company/[id]로 라우팅 (동적 라우팅)
   ↓
3. mockCompanies에서 해당 회사 데이터 조회
   ↓
4. mockVotes를 필터링하여 해당 회사의 투표만 추출
   ↓
5. 투표 데이터를 그룹/레이블/유저 기준으로 집계
   ↓
6. 탭 UI에 각 집계 데이터 표시
   ↓
7. 댓글 섹션에 mock 댓글 데이터 표시
```

---

## 7. 다음 단계

[Step 5: 투표 시스템(Voting) 및 인터랙티브 모달 구현]
랭킹 페이지에서 회사(예: 하이브)를 클릭하면 동적 라우팅(`/company/[id]`)으로 이동하여, 그 회사에 투표한 팬들을 **3가지 관점(그룹/레이블/유저)으로 집계**하고 **탭 UI**로 표시한 후, **댓글 기능**으로 커뮤니티 상호작용을 제공합니다.

---

## 2. 구현 요구사항

### [Task 1] 투표 데이터 구조 정의

**파일**: `src/lib/mock-data.ts`

Vote 인터페이스 추가:

```typescript
interface Vote {
  id: string;
  companyId: string; // 투표 대상 회사
  userId: string; // 투표한 유저
  groupId: string; // 유저가 팬인 아이돌 그룹
  labelId: string; // 그룹이 속한 레이블
  timestamp: number;
}

export const mockVotes: Vote[] = [
  // 최소 20개 이상의 투표 데이터
];
```

**데이터 설계**:

- 하이브(ID: 1)에 투표한 유저들: 뉴진스, 르세라핌, 벨플플 팬 섞임
- 각 그룹별 다양한 투표 분포 (예: 뉴진스 150표, 르세라핌 120표, 벨플플 80표)

---

### [Task 2] 동적 라우팅 페이지

**파일**: `src/app/company/[id]/page.tsx`

#### 1) Hero Header

- 회사명 + 순위 배지 (🏆 1위)
- 뒤로가기 버튼 (ChevronLeft 아이콘)
- 총 투표 수 카운터 (숫자 애니메이션)
- Gradient 배경 (Violet → Cyan)

#### 2) 탭 네비게이션 (3개 탭)

**탭 1: 그룹별 집계**

```
그룹명        투표 수   비율
뉴진스        150표    42.3%
르세라핌      120표    33.8%
벨플플        80표     22.6%
```

- 투표 수 내림차순 정렬

**탭 2: 레이블별 집계**

```
레이블명       투표 수   비율
어도어         270표    76.1%
플레이리스트   50표     14.1%
소스뮤직       35표     9.9%
```

- 투표 수 내림차순 정렬

**탭 3: 유저별 집계**

```
유저명         투표 수   선호 그룹
user123        5표      뉴진스
user456        3표      르세라핌
user789        2표      뉴진스
```

- 투표 수 내림차순 정렬

#### 3) 탭 UI 스펙

- 활성 탭: Primary Color 하단 보더 강조
- 탭 전환: `framer-motion` Fade-In 애니메이션
- 모바일: 카드 형식으로 변환

---

### [Task 3] 댓글 기능

**파일**: `src/components/features/comments/CommentSection/index.tsx`

#### 1) 댓글 입력 폼

- 유저명 입력 필드
- 댓글 내용 텍스트 영역 (최대 200자)
- 등록 버튼 (Primary Button)
- 로컬 상태(`useState`)로 관리

#### 2) 댓글 리스트

- 최신 댓글 상단 표시
- 각 댓글: 유저명 | 시간 | 내용 | 삭제 버튼
- Mock 데이터 5~10개
- Glass Panel 스타일 + 좌측 보더 Primary Color

---

### [Task 4] 페이지 애니메이션

**구현 위치**: 페이지 최상단 `motion.div`

- **진입**: 오른쪽에서 왼쪽으로 슬라이드 (`x: 100% → 0`)
- **퇴장**: 반대 방향 슬라이드

---

## 3. 디자인 스펙

| 항목        | 설정                                |
| ----------- | ----------------------------------- |
| Hero Header | Violet → Cyan 그래디언트            |
| Glass Panel | `rgba(10,10,10,0.8)` + `blur(20px)` |
| 테이블      | 호버 시 배경색 변화, 모바일은 카드  |
| 숫자 카운터 | 차오르는 애니메이션                 |

---

## 4. 파일 생성 체크리스트

- [ ] `src/lib/mock-data.ts` - Vote 타입 & 데이터 추가
- [ ] `src/app/company/[id]/page.tsx` - 동적 라우팅 페이지
- [ ] `src/app/company/[id]/company-detail.module.scss`
- [ ] `src/components/common/Tabs/index.tsx`
- [ ] `src/components/common/Tabs/Tabs.module.scss`
- [ ] `src/components/features/comments/CommentSection/index.tsx`
- [ ] `src/components/features/comments/CommentSection/CommentSection.module.scss`

---

## 5. 다음 단계

[Step 5: 투표 시스템(Voting) 및 인터랙티브 모달 구현]

#

`# 1. 현재 상황 및 목표 (Context & Goal)
우리는 [Step 3]에서 홈 화면의 회사 랭킹 리스트를 완성했습니다. 이제 사용자가 리스트의 회사를 클릭했을 때 이동할 **상세 분석 페이지**가 필요합니다.

당신의 목표는 Next.js의 **Dynamic Routing** 기능을 사용하여 `src/app/company/[id]` 경로를 구축하고, 다음 세 가지 관점에서 투표 데이터를 **집계 및 순위화**하며, 팬들이 댓글로 소통할 수 있는 **고품질 커뮤니티 페이지**를 구현하는 것입니다.

---

# 2. 실행 가이드: 단계별 작업 지시 (Step-by-Step Instructions)

### [Task 1] 투표 데이터 확장 (Vote Data Model)

`src/lib/mock-data.ts`를 확장하여 투표 데이터 구조를 정의하십시오.

**필요 데이터 구조 예시**:

```typescript
interface Vote {
  id: string;
  companyId: string; // 투표 대상 회사
  userId: string; // 투표한 유저
  groupId: string; // 유저가 팬인 아이돌 그룹
  labelId: string; // 그룹이 속한 레이블
  timestamp: number;
}

// 예시 투표 데이터 (최소 20개 이상)
export const mockVotes: Vote[] = [
  { id: '1', companyId: '1', userId: 'user1', groupId: 'g1', labelId: 'l1', timestamp: 1704067200 },
  { id: '2', companyId: '1', userId: 'user2', groupId: 'g2', labelId: 'l1', timestamp: 1704067300 },
  // ... 더 많은 투표 데이터
];
```

**데이터 팁**:

- 하이브(회사 ID: 1)에 투표한 유저들은 뉴진스, 르세라핌, 벨플플 팬이 섞여있도록.
- 각 그룹은 특정 레이블에 속하도록 설정 (예: 뉴진스→어도어, 르세라핌→어도어, 벨플플→플레이리스트 등).

### [Task 2] 동적 라우팅 페이지 구축 (Dynamic Page)

`src/app/company/[id]/page.tsx`를 생성하십시오.

**기능 및 UI 명세**:

#### **1) Hero Header Section**

- 회사 이름과 현재 순위 배지 표시 (예: "🏆 1위").
- 회사 로고 또는 Gradient 배경 (실사 금지, 추상 그래픽 사용).
- 좌측 상단에 '뒤로 가기(Back)' 버튼 (`lucide-react`의 `ChevronLeft`).
- 현재까지의 총 투표 수 대형 표시.

#### **2) 탭 네비게이션 (Tab Navigation)**

세 개의 탭으로 투표 데이터를 다차원 분석:

**탭 1: 그룹별 집계 (By Group)**

- 테이블 또는 카드 리스트로 표시.
- 컬럼: 그룹명 | 투표 수 | 비율(%)
- 투표 수 내림차순 정렬.
- 예시:
  ```
  뉴진스        | 150표 | 42.3%
  르세라핌      | 120표 | 33.8%
  벨플플        | 80표  | 22.6%
  ```

**탭 2: 레이블별 집계 (By Label)**

- 테이블 또는 카드 리스트로 표시.
- 컬럼: 레이블명 | 투표 수 | 비율(%)
- 투표 수 내림차순 정렬.
- 예시:
  ```
  어도어        | 270표 | 76.1%
  플레이리스트  | 50표  | 14.1%
  소스뮤직      | 35표  | 9.9%
  ```

**탭 3: 유저별 집계 (By User)**

- 테이블로 표시.
- 컬럼: 유저명 | 투표 수 | 선호 그룹
- 투표 수 내림차순 정렬 (다중 투표한 유저 상단).
- 예시:
  ```
  user123       | 5표  | 뉴진스
  user456       | 3표  | 르세라핌
  user789       | 2표  | 뉴진스
  ```

#### **3) 탭 UI 구현**

- 탭 버튼은 `src/components/common/Tabs/` 컴포넌트로 별도 제작.
- 활성 탭: Primary Color 하단 보더 강조.
- 비활성 탭: 회색 텍스트.
- 탭 전환 시 `framer-motion`으로 부드러운 Fade-In 애니메이션.

### [Task 3] 댓글 기능 (Comments Section)

탭 섹션 아래에 댓글 영역을 구현하십시오.

**기능 명세**:

1. **댓글 입력 영역 (Comment Form)**:
   - 사용자 이름 입력 필드 (또는 로그인 연동).
   - 댓글 내용 텍스트 영역 (최대 200자).
   - "등록" 버튼 (Primary Button).
   - 입력 후 로컬 상태(`useState`)에 저장 (실제 API 호출은 [Step 5]에서 다룰 예정).

2. **댓글 리스트 (Comment List)**:
   - 최신 댓글이 상단에 표시.
   - 각 댓글: 유저명 | 작성 시간 | 댓글 내용 | (삭제 버튼).
   - Mock 데이터로 5~10개의 초기 댓글 제공.
   - 스크롤 시 자동 로드(Infinite Scroll) 미지원 (간단한 고정 리스트).

3. **댓글 스타일**:
   - `_mixins.scss`의 `glass-panel` 스타일 재사용.
   - 각 댓글 카드는 좌측 테두리가 Primary Color로 강조.

### [Task 4] 페이지 전환 애니메이션 (Page Transition)

사용자가 리스트에서 상세 페이지로 들어올 때 자연스러운 UX를 제공해야 합니다.

- 컴포넌트 최상단을 `motion.div`로 감싸십시오.
- **진입 효과(Initial)**: 화면이 오른쪽에서 왼쪽으로 슬라이드(`x: '100%' -> 0`)되며 등장.
- **퇴장 효과(Exit)**: 반대로 슬라이드 아웃.

---

# 3. 디자인 디테일 (Design Details)

- **Hero Header**: Gradient 배경은 Primary Color(Violet)에서 Secondary Color(Cyan)로 점진적 변화.
- **Glass Panel**: 컨텐츠 영역의 배경은 투명도가 있는 검정(`rgba(10, 10, 10, 0.8)`)과 `backdrop-filter: blur(20px)`.
- **테이블 스타일**: 헤더는 짙은 배경, 행은 호버 시 약간의 배경색 변화. 모바일에서는 카드 형식으로 변환.
- **Stat Counter**: 총 투표 수는 숫자가 차오르는 애니메이션 효과 적용 (`framer-motion` `AnimatedCounter` 컴포넌트 제작 또는 라이브러리 활용).

---

# 4. 결과물 제출 요청 (Deliverables)

위 작업을 수행한 후, 다음 파일들의 **전체 코드**를 출력하십시오.

1. `src/app/company/[id]/page.tsx` (동적 라우팅 페이지)
2. `src/app/company/[id]/company-detail.module.scss` (스타일 정의)
3. `src/components/common/Tabs/index.tsx` (탭 컴포넌트)
4. `src/components/common/Tabs/Tabs.module.scss`
5. `src/components/features/comments/CommentSection/index.tsx` (댓글 영역 컴포넌트)
6. `src/components/features/comments/CommentSection/CommentSection.module.scss`
7. 수정이 필요하다면 업데이트된 `src/lib/mock-data.ts` (Vote 타입 및 데이터 추가)

지금 바로 회사 상세 페이지와 투표 분석 UI 구현을 시작하십시오.\`

---

[Next Step]

이 단계가 완료되면, 앱의 핵심 수익 모델이자 게이미피케이션 요소인 [Step 5: 투표 시스템(Voting) 및 모달 인터랙션 구현] 프롬프트를 진행하시면 됩니다.
