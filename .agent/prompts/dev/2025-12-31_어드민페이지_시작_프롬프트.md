### 핵심 요약

- **목적:** AI Agent(Cursor, Claude 등)가 한 번에 완벽한 결과물을 낼 수 있도록 맥락, 디자인, 코딩 규칙을 총망라한 **'마스터 프롬프트'** 작성.
- **보완 사항:**
  - **모바일 대응:** 사이드바의 반응형 동작(Mobile Toggle) 로직 추가.
  - **보안 강화:** Middleware를 통한 라우트 보호(Route Protection) 명시.
  - **데이터 정합성:** Supabase Generated Types(`database.types.ts`) 활용 강제.
  - **UX 강화:** Loading Skeleton 및 Error Boundary 구현 명시.

- **사용법:** 아래 프롬프트 내용을 그대로 복사하여 AI 에디터(Cursor Composer 등)에 붙여넣으십시오.

---

### 🏛️ PLOZEN Admin 개발을 위한 마스터 프롬프트 (v1.1)

이 프롬프트는 현재까지 논의된 디자인 시스템, 메뉴 구조, 기술 스택을 완벽하게 통합하고, AI가 실수하기 쉬운 **타입 안전성**, **반응형 처리**, **상태 관리** 로직을 전문가 수준으로 보강하여 작성되었습니다.

````markdown
# Role Definition

당신은 Google 및 Tesla 출신의 Senior Frontend Engineer이자 UI/UX Expert입니다.
현재 `packages/plozen` 패키지 내에서 Next.js 16 (App Router) 기반의 **관리자 페이지(Admin Dashboard)**를 구축하는 임무를 맡았습니다.

아래의 [Context], [Design System], [Requirements]를 완벽하게 숙지하고, 단계별로 코드를 작성하십시오.

---

# 1. Project Context

- **Target Package:** `packages/plozen`
- **Framework:** Next.js 16.1.1 (App Router), React 19
- **Language:** TypeScript
- **Styling:** SCSS Modules (`.module.scss`) + Global SCSS Variables
- **Icons:** `lucide-react`
- **Theme:** `next-themes` (System/Light/Dark)
- **Auth/DB:** Supabase (SSR)
- **Type Definition:** `database.types.ts` (Supabase Generated)

# 2. Design System & UI Concepts

**Core Concept:** "Focus on Data, Minimal Distraction"
관리자 페이지는 정보의 가독성을 최우선으로 하며, PLOZEN의 브랜드 컬러는 **핵심 동작(CTA)과 활성 상태(Active)**에만 제한적으로 사용합니다.

### 2.1 Color Palette (SCSS Variables 매핑 필요)

**Light Mode (Clean & White)**

- Background: `#FFFFFF` (Pure White)
- Surface: `#FFFFFF` (Card/Sidebar)
- Text: `#111111` (Deep Void)
- Border: `#E5E5E5` (Very Subtle)
- **Accent:** `#a0dd09` (Deep Lime) - 텍스트 가독성 확보

**Dark Mode (Cyberpunk & Void)**

- Background: `#111111` (Deep Void)
- Surface: `#1E1E1E` (Dark Gray)
- Text: `#FFFFFF` (Pure White)
- Border: `#333333`
- **Accent:** `#D6FF00` (Bio Lime) - Neon Effect

### 2.2 Layout Strategy

- **Sidebar:** Fixed Width (260px), Left side. (Mobile: Hidden/Drawer)
- **Header:** Sticky Top (64px), Blur Effect.
- **Content:** Fluid Width, Padding 24px.
- **Visual:** "Border-Less" 느낌을 주되, 1px의 미세한 Border로 구획을 명확히 구분. Shadow 사용 지양.

---

# 3. Directory Structure (Route Groups)

일반 사용자(`(site)`)와 관리자(`(admin)`)를 물리적으로 격리합니다.

```text
packages/plozen/src/
├── app/
│   ├── (site)/             # Existing User Pages
│   └── (admin)/            # [NEW] Admin Area
│       ├── layout.tsx      # Admin Root Layout (Auth Check + ThemeProvider)
│       └── admin/
│           ├── layout.tsx  # Dashboard Layout (Sidebar + Header)
│           ├── page.tsx    # Dashboard Main (KPIs)
│           ├── loading.tsx # [NEW] Global Admin Skeleton
│           ├── error.tsx   # [NEW] Global Admin Error Boundary
│           ├── quotes/     # Project Quotes (프로젝트 의뢰)
│           ├── inquiries/  # General Inquiries (일반 문의)
│           ├── settings/   # System Settings (설정)
│           └── login/      # Admin Login
├── components/
│   └── admin/              # [NEW] Admin Atomic Components
│       ├── Sidebar.tsx
│       ├── Header.tsx
│       └── ...
├── config/
│   └── admin-navigation.ts # [NEW] Menu Configuration
└── styles/
    └── admin-theme.scss    # [NEW] Admin Specific Variables
```
````

---

# 4. Functional Requirements (Step-by-Step)

### Step 1: Configuration & Styles

1. `packages/plozen/src/config/admin-navigation.ts` 생성

- 메뉴 구조: 대시보드(Dashboard), 프로젝트 의뢰(Quotes), 일반 문의(Inquiries), 설정(Settings).
- 각 아이콘은 `lucide-react` 사용.
- 데이터 구조: `title`, `href`, `icon`, `matchPath` 포함.

2. `packages/plozen/src/styles/admin-theme.scss` 생성

- `:root` 및 `[data-theme='dark']`를 사용하여 위 [Design System]의 컬러 변수 정의.
- 변수명 규칙: `--admin-bg`, `--admin-accent`, `--admin-border` 등.

### Step 2: Layout Implementation

1. `packages/plozen/src/app/(admin)/layout.tsx`

- `<html>`, `<body>` 태그 및 `ThemeProvider` 설정.
- 전역 스타일(`main.scss`)과 Admin 테마(`admin-theme.scss`) 로드.

2. `packages/plozen/src/components/admin/Sidebar.tsx`

- `usePathname`을 이용한 활성 상태(Active State) 스타일링.
- **Deep Lime Point:** 활성 메뉴 우측에 3px Bar 또는 텍스트 컬러 변경.
- 하단에 로그아웃 버튼 배치.

3. `packages/plozen/src/components/admin/Header.tsx`

- 현재 경로에 따른 Breadcrumb/Title 표시.
- `ThemeToggle` 컴포넌트 재사용.
- 모바일 환경에서 사이드바를 열 수 있는 'Hamburger Menu' 버튼 구현 (Mobile Only).

### Step 3: Page Scaffolding

1. `/admin` (Dashboard): 빈 껍데기 페이지 생성 (추후 위젯 추가).
2. `/admin/quotes`, `/admin/inquiries`: 기본적인 리스트 테이블 레이아웃 스캐폴딩.
3. **Middleware/Auth Protection:** `/admin` 경로 접근 시 Supabase Session 확인 로직 추가 (의사 코드 또는 주석으로 위치 명시).
4. **Robust UX:**

- `app/(admin)/admin/loading.tsx`: 대시보드 로딩 시 보여줄 스켈레톤 UI(Skeleton UI) 구현.
- `app/(admin)/admin/error.tsx`: 데이터 로드 실패 시 보여줄 에러 바운더리(Error Boundary) 및 재시도(Retry) 버튼 구현.

---

# 5. Coding Standards & Constraints

1. **Strict TypeScript & Supabase Types:**
   - `any` 타입 사용 금지.
   - Supabase 데이터 통신 시, 임의의 Interface를 정의하지 말고 **반드시 `database.types.ts` (또는 해당 경로)의 `Database` 타입을 import**하여 제네릭으로 주입하십시오. (SSOT 준수)
   - 예: `SupabaseClient<Database>`

2. **Component Strategy:**
   - 기존 `components/ui`에 Atomic 컴포넌트(Button, Input 등)가 있다면 최대한 재사용하여 디자인 일관성을 유지하십시오.
   - 단, Admin 고유의 디자인이나 기능이 필요한 경우에만 `components/admin` 내에 별도 구현하거나 래퍼(Wrapper)를 만드십시오.

3. **Server vs Client:**

- `Sidebar`, `Header` 등 인터랙션이 있는 컴포넌트는 `"use client"` 명시.
- 데이터 페칭이 필요한 페이지는 기본적으로 **Server Component**로 구성.

4. **Clean Code:**

- 하드코딩 금지 (모든 텍스트와 설정값은 상수 또는 config 파일로 분리).
- 스타일은 인라인 스타일 대신 SCSS Module 또는 Tailwind Utility Class(`cn` 함수 활용) 사용.

5. **Error Handling:**

- 이미지 로드 실패, 데이터 페칭 실패 시 Fallback UI 고려.
- 에러 발생 시 사용자에게 친절한 메시지 제공 (단순 "Error" 출력 지양).

---

# Action Item

위 명세를 바탕으로 **Step 1(설정 및 스타일)**과 **Step 2(레이아웃 컴포넌트 및 페이지 구조)**를 구현하는 코드를 작성해주세요.
파일별로 명확하게 구분하여 작성하고, 코드를 작성하기 전에 변경되는 파일 목록을 먼저 요약해주세요.

```

---

### 💡 파트너를 위한 추가 가이드

1.  **AI에게 입력 시:** 위 코드 블록 전체를 복사해서 붙여넣으세요.
2.  **검증 포인트:** AI가 코드를 생성하면 다음 사항을 확인하세요.
    * `layout.tsx`가 `(admin)` 폴더 내에 정확히 위치했는가?
    * `loading.tsx`와 `error.tsx`가 포함되었는가?
    * Supabase 클라이언트 생성 시 `<Database>` 제네릭이 사용되었는가?
3.  **다음 단계:** 이 프롬프트 실행이 완료되면, "이제 대시보드 메인 위젯을 만들어줘"라고 이어서 요청하시면 됩니다.

```
